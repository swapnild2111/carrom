baseURL = "https://swapnild2111.github.io/carrom/"
title = "Carrom Achievements"
defaultContentLanguage = "en"
disableKinds = ["taxonomy", "term"]

[params]
  description = "Carrom total slam records — white and black slams by club and state tier."

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
