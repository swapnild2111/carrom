baseURL = "https://swapnild2111.github.io/carrom/"
title = "Carrom Achievements"
defaultContentLanguage = "en"
disableKinds = ["taxonomy", "term"]

[params]
  description = "Carrom player achievement tracker — slams, tournaments, rankings, and player profiles."

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
