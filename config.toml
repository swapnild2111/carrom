baseURL = "https://swapnild2111.github.io/carrom/"
title = "Carrom Achievements"
defaultContentLanguage = "en"
disableKinds = ["taxonomy", "term"]

[params]
  description = "Carrom slam leaderboard — white and black slam counts in club, state, and YouTube matches."

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
